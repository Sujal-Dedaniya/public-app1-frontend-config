apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: app1-ns
spec:
  replicas: 2
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: sujaldedaniya1014/app1-frontend:v1.0.0
          ports:
            - containerPort: 4000
          env:
            - name: BACKEND_URL
              value: http://backend-svc:5000/api/message